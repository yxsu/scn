#pragma once

#include "matrix.hpp"
/*
class SqrMatrix : public Matrix
{

public:

   SqrMatrix();

   SqrMatrix(size_t size);

   SqrMatrix(const SqrMatrix &other);

   ~SqrMatrix();

   virtual void Create(size_t size);

   virtual void Create(const SqrMatrix &other);

public:

   inline size_t GetSize() const;

   inline pVector GetDiagonal();

   inline void SetDiagonal(const pVector source);

protected://data

   size_t size;
   
};

size_t SqrMatrix::GetSize() const
{
   return size;
}
*/
